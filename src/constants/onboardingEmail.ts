export const APPROVED_EMAIL_PROVIDER_EXAMPLES =
  "Gmail, Outlook, Yahoo, iCloud, or Proton Mail";

export const ONBOARDING_EMAIL_REQUIREMENT =
  `To receive your initial QORT, use a personal email from a major provider ` +
  `(e.g. ${APPROVED_EMAIL_PROVIDER_EXAMPLES}). Temporary or disposable email ` +
  `addresses are not supported.`;
