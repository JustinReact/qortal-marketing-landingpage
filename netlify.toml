[build]
  command = "npm run build"

[[plugins]]
  package = "@netlify/plugin-nextjs"  # Use the Netlify Next.js plugin to support SSR and dynamic routes

[context.production.environment]
  NODE_VERSION = "20"
